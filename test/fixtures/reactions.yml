# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  mode: like
  user: two
  reactable: one (Comment)

two:
  mode: like
  user: two
  reactable: one (Post)

three:
  mode: like
  user: one
  reactable: last_by_created_at (Post)
